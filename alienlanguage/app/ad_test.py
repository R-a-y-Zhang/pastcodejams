def runtest(wordDict, lettersArray):
	return runtest(0, [], wordDict, lettersArray, 0, 0)

def runtest(wordsFound, wordCandidates, wordDict, lettersArray, i, j):
	for i in range (lettersArray):
		if len(lettersArray[i] == 1:
			for i in range(len(wordCandidates)):
				wordCandidates[i] = wordCandidates[i] + lettersArray[i]
	
